export interface Product {
  id: number;
  nome: string;
  preco: number;
  quantidade: number;
  foto: string;
  descricao: string;
  status: string;
}

export interface ProductData {
  nome: string;
  preco: number;
  quantidade: number;
  foto: string;
  descricao: string;
  status: string;
}